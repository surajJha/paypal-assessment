/**
 * @class BaseTextItem this class acts as the Base class for
 * different types of text items like Entity, link and so on
 */
class BaseTextItem {
    format(text) {

    }
}

module.exports = BaseTextItem;
